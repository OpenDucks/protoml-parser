@help
=name:status
=docs:
`@status:...` defines the document lifecycle status.

Common values are `draft`, `review`, `approved`, `active`, or `archived`.
The value is stored in the document metadata and can be referenced via `@@ref=meta:status`.

=examples:
@status:review
