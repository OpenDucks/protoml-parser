@help
=name:signature
=docs:
`@@signature=id` inserts a formatted signature block inside the `@meeting` block.

The referenced entry must exist in the `@signatures` block.

=examples:
@@signature=lead
