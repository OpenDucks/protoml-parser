@help
=name:macros
=docs:
`protoparser macros <pml_file>` lists registered and used macros for a ProtoML document, including recursively imported `.pml` files.

It is recommended to wrap the file path in double quotes.

=examples:
protoparser macros "Meeting.pml"
