@help
=name:attachments
=docs:
`@attachments` defines linked attachment entries that are rendered as an own section in HTML output.

Entries use list syntax. You can write plain text or `label|path-or-url`.

=examples:
@attachments
- Demo graph|main-demo-graph.mmd
- Contract appendix|appendix.pdf
