@help
=name:protocol
=docs:
`@protocol "..."` sets the HTML document title and the top page heading.

It supports placeholders such as `{{date}}` and is mainly relevant for HTML and PDF rendering.

=examples:
@protocol "Protocol - {{date}}"
