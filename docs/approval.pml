@help
=name:approval
=docs:
`@@approval=id` inserts a formatted approval block inside the `@meeting` block.

The referenced entry must exist in the `@approvals` block.

=examples:
@@approval=security
