@help
=name:tag_sources
=docs:
`@tag_sources` defines source `.pml` files for shared tag analysis.

It is intended for shared tag files such as `_tags.pml` and is ignored during normal document rendering.

=examples:
@tag_sources
- "meetings/week1.pml"
- "meetings/week2.pml"
