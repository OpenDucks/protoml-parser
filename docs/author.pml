@help
=name:author
=docs:
`@author:...` defines the author of the current document.

It is useful for protocols, contracts, onboarding files, RCA documents, and versioned internal documentation.

The value can be referenced inside `@meeting` via `@@ref=meta:author`.

=examples:
@author:Jane Doe
