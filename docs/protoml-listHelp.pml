@help
=name:protoml-listHelp
=docs:
Lists all available documentation modules from the `/docs` folder.

Each `.pml` doc file must start with `@help` and include at least a `=name:` and `=docs:` field.

=examples:
protoparser --listDocs
