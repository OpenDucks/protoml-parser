@help
=name:output
=docs:
`@@output=name` and `@@import=name` insert previously registered import content into the `@meeting` block.

For `.pml` imports the imported meeting section is inserted after parsing and merging.

=examples:
@@import=snippet
@@output=appendix
