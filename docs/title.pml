@help
=name:title
=docs:
`@title "..."` is used for shared tag files and tag statistics output.

It allows a custom report title instead of falling back to the file name.

=examples:
@title "Shared Project Tags"
