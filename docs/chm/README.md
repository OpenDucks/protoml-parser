# CHM Help

This directory contains the native Windows HTML Help project for ProtoML.

It includes:

- `protoml-help.hhp` as the HTML Help Workshop project file
- `TOC.hhc` as the table of contents definition
- `Index.hhk` as the keyword index
- `toc.html` as the visible contents start page
- `html_docs/` with the generated help topic pages
- `protoml-help.chm` as a compiled CHM build when generated on Windows

## Build

Regenerate the CHM project files:

```bash
npm run build:chm:project
```

Compile the CHM file on Windows with HTML Help Workshop:

```bash
npm run build:chm
```

## Notes

- The files in this directory are generated by `src/utils/build-chm-docs.js`.
- If you want to change the structure or topic content, update the generator rather than editing generated files by hand.
- The source ProtoML help topics still live in `docs/*.pml`.
