# ProtoML Macro Packs

This directory stores project-local external macro packs.

- Installed packs live in `macro-packs/`
- The project definition file is `../protoml.macros.json`
- The lock file is `protoml.macros.lock.json`
